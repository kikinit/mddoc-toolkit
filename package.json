{
  "name": "@kikinit/readme-toolkit",
  "version": "1.1.0",
  "description": "A toolkit for parsing readme files from code repositories and NPM packages",
  "main": "index.js",
  "type":"module",
  "scripts": {
  "start": "node --env-file=.env index.js",
  "lint": "npx eslint . --ext .js || exit 0",
  "lint:fix": "npx eslint . --ext .js --fix || exit 0"
},
  "repository": {
    "type": "git",
    "url": "git+https://github.com/legitmattias/readme-toolkit.git"
  },
  "author": "mu222cu@student.lnu.se",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^9.10.0",
    "prettier": "^3.3.3"
  },
  "dependencies": {
  }
}
